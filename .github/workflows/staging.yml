name: Staging Deploy

on:
  push:
    branches:
      - staging

env:
  IMAGE_NAME: ghcr.io/$(echo ${{github.REPOSITORY}} | tr '[:upper:]' '[:lower:]')/${{ github.ref_name }}:latest

jobs:
  Publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checking out
        uses: actions/checkout@v4.1.7
      - name: Build
        run: docker image build -t ${{env.IMAGE_NAME}} --build-arg MODE=staging .
      - name: Docker login
        uses: docker/login-action@v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Push
        working-directory: src
        run: docker image push ${{env.IMAGE_NAME}}